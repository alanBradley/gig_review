<%= simple_form_for @gig do |f| %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt => "Select Category") %>
  <%= f.input :title, label: "Gig Name" %>
  <%= f.input :description %>
  <%= f.input :promoter %>
  <%= f.button :submit %>
<% end %>